import styled, { css } from 'styled-components';
const ButtonList = styled.div`
  display: flex;
  z-index: 3;
  position: fixed;
  right: 10px;
  bottom: 70px;
  flex-direction: column;
  gap: 6px;
`;

export { ButtonList };
