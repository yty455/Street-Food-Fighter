server:
  port: 8081

spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/test
    username: sa
    password:
    driver-class-name: org.h2.Driver
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
        show_sql: true
    open-in-view: false
  mvc:
    static-path-pattern: /static/**
  data:
    User_Server: ""
    Order_Server: "http://localhost:8080"
    Notification_Server: ""

logging:
  level:
    org.hibernate.sql: debug
    org.hibernate.type: trace

